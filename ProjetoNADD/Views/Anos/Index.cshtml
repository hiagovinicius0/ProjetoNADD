@model IEnumerable<ProjetoNADD.Models.Ano>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Ano_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Ano_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Ano_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
