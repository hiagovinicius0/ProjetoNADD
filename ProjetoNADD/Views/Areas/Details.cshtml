@model ProjetoNADD.Models.Area

@{
    ViewData["Title"] = "Detalhes";
}

<h2>Detalhes</h2>

<div>
    <h4>Área</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Nome_Area)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Nome_Area)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id_Area">Editar</a> |
    <a asp-action="Index">Voltar para Áreas</a>
</div>
