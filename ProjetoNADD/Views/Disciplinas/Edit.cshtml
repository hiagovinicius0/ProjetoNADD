@model ProjetoNADD.Models.Disciplina

@{
    ViewData["Title"] = "Editar";
}
@section Styles{
    <environment include="Development">
        <link href="~/lib/select2/css/select2.css" rel="stylesheet" />
    </environment>
    <environment include="Development">
        <script src="~/lib/jquery/dist/jquery.js"></script>
        <script src="~/lib/select2/js/select2.js"></script>
    </environment>
}
<h2>Editar</h2>

<h4>Disciplina</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id_Disciplina"  id="Id_Disciplina"/>
            <div class="form-group">
                <label asp-for="Nome_Disciplina" class="control-label"></label>
                <input asp-for="Nome_Disciplina" class="form-control" />
                <span asp-validation-for="Nome_Disciplina" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Periodo_Disciplina" class="control-label"></label>
                <input asp-for="Periodo_Disciplina" class="form-control" />
                <span asp-validation-for="Periodo_Disciplina" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Ano_Disciplina" class="control-label"></label>
                <input asp-for="Ano_Disciplina" class="form-control" />
                <span asp-validation-for="Ano_Disciplina" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CursoId" class="control-label"></label>
                <select asp-for="CursoId" class="form-control" asp-items="ViewBag.CursoId"></select>
                <span asp-validation-for="CursoId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label for="Professor_id" class="control-label">Professor</label>
                <select name="Professor_id" class="form-control" id="Professor_id" asp-items="ViewBag.ProfessoresId" multiple="multiple"></select>
                <input type="hidden" id="marcados" value="ViewBag.ProfessoresMarcados"/>
            </div>
            <div class="form-group">
                <button type="button" class="btn btn-primary" onclick="Editar();"><span class="glyphicon glyphicon-save"></span> Salvar</button>
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Voltar para Disciplinas</a>
</div>
@section ScriptPage{
    <environment include="Development">
        <link href="~/lib/select2/css/select2.css" rel="stylesheet" />
    </environment>
    <environment include="Development">
        <script src="~/lib/jquery/dist/jquery.js"></script>
        <script src="~/lib/select2/js/select2.js"></script>
    </environment>
    <script>
        function CallAjax(serviceFullPath, dataString, sucessCallBack) {
            $.ajax({
                type: "POST",
                contentType: "application/json; charset=utf-8",
                url: serviceFullPath,
                data: dataString,
                dataType: "json",
                async: true,
                success: sucessCallBack
            });
        }
        $(document).ready(function () {
            jQuery('#Professor_id').select2({
                placeholder: "Selecione os Professores",
                allowClear: true
            });
            $('#Professor_id').val()
            var id = $('#Id_Disciplina').val();
            ProfessoresMarcados();
        });
        function Editar() {
            var professores = $('#Professor_id').val()
            var nome
            if (professores.length > 0) {
                professores2 = [];
                for (var i = 0; i < professores.length; i++) {
                    professores2.push(Number(professores[i]))
                }
                professores = professores2;
            }

            var dataString = {
                Id_Disciplina: $('#Id_Disciplina').val(),
                professores: professores,
                Nome_Disciplina: $('#Nome_Disciplina').val(),
                Periodo_Disciplina: $('#Periodo_Disciplina').val(),
                Ano_Disciplina: $('#Ano_Disciplina').val(),
                CursoId: $('#CursoId').val()
            }
            console.log(dataString.professores);
            $.ajax({
                type: "POST",
                url: "Edit",
                data: dataString,
                success: function (dados) {
                    console.log(dados)
                }
            });
        }
        function ProfessoresMarcados() {
            $.ajax({
                type: "POST",
                url: "../ProfessoresMarcados",
                data: {
                    disciplina: $('#Id_Disciplina').val()
                    
                },
                success: function (dados) {
                    if (dados.length > 0) {
                        var array = [];
                        for (var i = 0; i < dados.length; i++) {
                            array.push(dados[i].id)
                        }
                        $("#Professor_id").val(array).trigger("change");
                    }
                }
            });
        }
    </script>
}
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
