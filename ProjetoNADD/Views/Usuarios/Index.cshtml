@model IEnumerable<ProjetoNADD.Models.Usuario>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Login_Usuario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nome_Usuario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Senha_Usuario)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Login_Usuario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nome_Usuario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Senha_Usuario)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_Usuario">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_Usuario">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_Usuario">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
